server:
  port: ${SERVER_PORT:8080}

spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: ${SPRING_APPLICATION_NAME:gateway-service}

  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: ${GATEWAY_CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost:4201,https://stockpro-frontend.netlify.app}
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true
      routes:
        - id: auth-service
          uri: ${GATEWAY_ROUTE_AUTH_URI:lb://AUTHSERVICE}
          predicates:
            - Path=/api/v1/auth/**

        - id: auth-service-oauth2
          uri: ${GATEWAY_ROUTE_AUTH_URI:lb://AUTHSERVICE}
          predicates:
            - Path=/oauth2/**,/login/oauth2/**

        - id: product-service
          uri: ${GATEWAY_ROUTE_PRODUCT_URI:lb://PRODUCT-SERVICE}
          predicates:
            - Path=/api/v1/products/**

        - id: warehouse-service
          uri: ${GATEWAY_ROUTE_WAREHOUSE_URI:lb://WAREHOUSE-SERVICE}
          predicates:
            - Path=/api/v1/warehouses/**

        # Supplier Service
        - id: supplier-service
          uri: ${GATEWAY_ROUTE_SUPPLIER_URI:lb://SUPPLIER-SERVICE}
          predicates:
            - Path=/api/v1/suppliers/**

        # Stock Movement Service
        - id: stockmovement-services
          uri: ${GATEWAY_ROUTE_MOVEMENT_URI:lb://stockmovement-services}
          predicates:
            - Path=/api/v1/movements/**,/api/v1/movements

        # Purchase Service
        - id: purchase-service
          uri: ${GATEWAY_ROUTE_PURCHASE_URI:lb://purchase-service}
          predicates:
            - Path=/api/v1/purchase-orders/**

        # Payment Service
        - id: payment-service
          uri: ${GATEWAY_ROUTE_PAYMENT_URI:lb://payment-service}
          predicates:
            - Path=/api/v1/payments/**

        # Analytics Service
        - id: analytics-service
          uri: ${GATEWAY_ROUTE_ANALYTICS_URI:lb://analytics-service}
          predicates:
            - Path=/api/v1/analytics/**

        # Alert Service
        - id: alert-service
          uri: ${GATEWAY_ROUTE_ALERT_URI:lb://ALERT-SERVICE}
          predicates:
            - Path=/api/v1/alerts,/api/v1/alerts/**

        # Swagger Docs Aggregation Routes
        - id: auth-docs
          uri: ${GATEWAY_ROUTE_AUTH_URI:lb://AUTHSERVICE}
          predicates:
            - Path=/auth/v3/api-docs, /auth/v3/api-docs/**
          filters:
            - 'RewritePath=/auth/(.*), /$1'

        - id: product-docs
          uri: ${GATEWAY_ROUTE_PRODUCT_URI:lb://PRODUCT-SERVICE}
          predicates:
            - Path=/product/v3/api-docs, /product/v3/api-docs/**
          filters:
            - 'RewritePath=/product/(.*), /$1'

        - id: warehouse-docs
          uri: ${GATEWAY_ROUTE_WAREHOUSE_URI:lb://WAREHOUSE-SERVICE}
          predicates:
            - Path=/warehouse/v3/api-docs, /warehouse/v3/api-docs/**
          filters:
            - 'RewritePath=/warehouse/(.*), /$1'

        - id: supplier-docs
          uri: ${GATEWAY_ROUTE_SUPPLIER_URI:lb://SUPPLIER-SERVICE}
          predicates:
            - Path=/supplier/v3/api-docs, /supplier/v3/api-docs/**
          filters:
            - 'RewritePath=/supplier/(.*), /$1'

        - id: movement-docs
          uri: ${GATEWAY_ROUTE_MOVEMENT_URI:lb://stockmovement-services}
          predicates:
            - Path=/movement/v3/api-docs, /movement/v3/api-docs/**
          filters:
            - 'RewritePath=/movement/(.*), /$1'

        - id: purchase-docs
          uri: ${GATEWAY_ROUTE_PURCHASE_URI:lb://purchase-service}
          predicates:
            - Path=/purchase/v3/api-docs, /purchase/v3/api-docs/**
          filters:
            - 'RewritePath=/purchase/(.*), /$1'

        - id: payment-docs
          uri: ${GATEWAY_ROUTE_PAYMENT_URI:lb://payment-service}
          predicates:
            - Path=/payment/v3/api-docs, /payment/v3/api-docs/**
          filters:
            - 'RewritePath=/payment/(.*), /$1'

        - id: analytics-docs
          uri: ${GATEWAY_ROUTE_ANALYTICS_URI:lb://analytics-service}
          predicates:
            - Path=/analytics/v3/api-docs, /analytics/v3/api-docs/**
          filters:
            - 'RewritePath=/analytics/(.*), /$1'

        - id: alert-docs
          uri: ${GATEWAY_ROUTE_ALERT_URI:lb://ALERT-SERVICE}
          predicates:
            - Path=/alert/v3/api-docs, /alert/v3/api-docs/**
          filters:
            - 'RewritePath=/alert/(.*), /$1'

springdoc:
  swagger-ui:
    path: ${SPRINGDOC_SWAGGER_UI_PATH:/swagger-ui.html}
    urls:
      - name: Auth Service
        url: /auth/v3/api-docs
      - name: Product Service
        url: /product/v3/api-docs
      - name: Warehouse Service
        url: /warehouse/v3/api-docs
      - name: Supplier Service
        url: /supplier/v3/api-docs
      - name: Movement Service
        url: /movement/v3/api-docs
      - name: Purchase Service
        url: /purchase/v3/api-docs
      - name: Payment Service
        url: /payment/v3/api-docs
      - name: Analytics Service
        url: /analytics/v3/api-docs
      - name: Alert Service
        url: /alert/v3/api-docs

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
    register-with-eureka: ${EUREKA_CLIENT_REGISTER_WITH_EUREKA:true}
    fetch-registry: ${EUREKA_CLIENT_FETCH_REGISTRY:true}
  instance:
    prefer-ip-address: ${EUREKA_INSTANCE_PREFER_IP_ADDRESS:true}

management:
  endpoints:
    web:
      exposure:
        include: ${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE:health,info}
